{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Odin Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe", 
            "args": [""], 
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "stopOnEntry": false,
        }
    ]
}